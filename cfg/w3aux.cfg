# W3AUX -- the composer blob (docs/42 v2): linked at $A9C0 and pasted by
# make_512k into PAGE 8's dead level-header region; the W3 sprite slice is
# mirrored at $A600. The bank-1 tail ($B540+, the W3 bg charset) stays intact.
MEMORY {
    AUXM: start $A9C0, size $0B80, type rw, file %O, fill no;
}
SEGMENTS {
    AUX: load AUXM type rw;
}
