# W3AUX -- the aux-page blob (docs/42): linked standalone at $A620; make_512k
# pastes the binary into PAGE 8 after the bank-1 prefix copy.
MEMORY {
    AUXM: start $A620, size $1960, type rw, file %O, fill no;
}
SEGMENTS {
    AUX: load AUXM type rw;
}
